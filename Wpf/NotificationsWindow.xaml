<Window x:Class="Cliver.CisteraNotification.NotificationsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cliver.CisteraNotification"
        mc:Ignorable="d"
        Title="InformWindow" Height="100" Width="400" 
        WindowStyle="None" ResizeMode="CanResize" AllowsTransparency="True" 
        Background="Transparent" Topmost="False" ShowInTaskbar="False"  >
    <Grid>
        <Grid.RowDefinitions>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
        </Grid.ColumnDefinitions>
        <Button x:Name="button" Content="Button" HorizontalAlignment="Right" VerticalAlignment="Top" Width="75"/>
        <StackPanel Name="infos" HorizontalAlignment="Stretch" Height="auto" Margin="0,27,0,0" VerticalAlignment="Top"  Orientation="Vertical"/>

    </Grid>
</Window>
