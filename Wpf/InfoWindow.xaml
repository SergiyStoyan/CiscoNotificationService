<Window x:Class="Cliver.CisteraNotification.InfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cliver.CisteraNotification"
        mc:Ignorable="d" 
        Title="InfoWindow" Height="auto" Width="400" 
        WindowStyle="None" ResizeMode="NoResize" AllowsTransparency="True" 
        Background="Beige" Topmost="False" ShowInTaskbar="False" SizeToContent="Height" >
    <Window.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard Storyboard.TargetProperty="Opacity">
                    <DoubleAnimation Name="condensing" Duration="00:00:3" From="0" To="1"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard >
                    <DoubleAnimation Name="sliding" Duration="0:0:3" Storyboard.TargetProperty="Left" From="920" To="100" AccelerationRatio=".1"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>
    <Grid x:Name="grid">
    </Grid>
</Window>
